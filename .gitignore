# Fonts werden von build.sh geladen (SIL OFL), nicht eingecheckt
assets/fonts/*
!assets/fonts/.gitkeep

# Python
__pycache__/
*.pyc

# Build-Zwischendateien
deliverables/_report_raw.pdf

# OS
.DS_Store
Thumbs.db

# Browser-Automation-Artefakte (Playwright MCP: Snapshots/Logs)
.playwright-mcp/

# Screenshot-Artefakte (regenerierbar; nur README/Checkliste wird versioniert)
docs/screenshots/*.png

# Secrets — niemals einchecken (echte Zugangsdaten)
.env
.env.local
secrets/
!.env.example
